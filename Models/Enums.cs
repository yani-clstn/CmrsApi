namespace CmrsApi.Models
{
    public enum ReservationType
    {
        RecurringClass,
        OneTimeEvent
    }

    public enum ReservationStatus
    {
        Pending,
        Approved,
        Cancelled
    }
}
